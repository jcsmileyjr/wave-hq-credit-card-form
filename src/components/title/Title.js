import React from "react";
import "./title.css";

const Title = () => {
    return(
        <section className="title__container--style">
            <h1 className="title__content--style">Pay Invoice</h1>
        </section>
    );
}

export default Title;